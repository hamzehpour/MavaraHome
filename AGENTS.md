# AGENTS.md — قواعد کار روی این پروژه

> این فایل اولین چیزیه که هر توسعه‌دهنده‌ی تازه‌وارد یا هر ابزار هوش مصنوعی
> (Claude Code، Cursor، Copilot، Codex، Aider و…) باید بخونه. کوتاهه چون
> فقط **قوانینی** رو می‌گه که شکستنشون سیستم رو خراب می‌کنه. توضیح کامل هر
> چیزی در `docs/` است — از `docs/README.md` شروع کن.

## این پروژه چیست

سامانه‌ی رزرو و مدیریت محتوای «خانه ماورا»، یک مجموعه‌ی هنری/تئاتری در تهران.
یک ربات تلگرام + یک وب‌سایت که **یک دیتابیس مشترک** دارند: مخاطب می‌تواند از
هر دو مسیر بلیت رزرو کند، و ادمین از هر دو مسیر مدیریت کند. پرداخت **درگاه
بانکی ندارد** — کارت‌به‌کارت است و مشتری عکس رسید را می‌فرستد تا ادمین
تأیید کند.

- زبان اصلی محصول: فارسی (RTL). انگلیسی به‌عنوان زبان دوم روی سایت پشتیبانی می‌شود.
- تقویم: جلالی.
- پشته: Python (aiogram + `http.server` استاندارد) · SQLite · HTML/CSS/JS ساده بدون فریمورک و بدون مرحله‌ی build.

## ۱۲ قانون طلایی

این‌ها اختراع سلیقه‌ای نیستند؛ هرکدام یا در کد به‌صراحت نوشته شده‌اند یا از
یک باگ واقعی که قبلاً رخ داده بیرون آمده‌اند.

1. **منطق تجاری فقط در `bot/services/`.** `bot/api/server.py` و
   `bot/handlers/` فقط لایه‌ی انتقال‌اند: ورودی را می‌خوانند، یک تابع سرویس
   را صدا می‌زنند، خروجی را قالب‌بندی می‌کنند. اگر داری در سرور یا هندلر
   `if` تجاری می‌نویسی، جایش آنجا نیست.

2. **تغییر وضعیت رزرو فقط با `set_status_if()` یا `set_status_if_any()`.**
   این‌ها `UPDATE … WHERE id=? AND status IN (…)` هستند — وضعیت را در همان
   جمله‌ای که عوض می‌کند بررسی می‌کنند. اگر `False`/`None` برگشت یعنی
   «کس دیگری زودتر رسید»، که خطا نیست و نباید retry شود. `set_status()`
   خام فقط برای `expired` مجاز است (زمان‌بند تنها نویسنده‌ی آن وضعیت است).
   `admin_cancel_reservation()` تا v18 استثنا بود و همین باعث می‌شد بتواند
   وضعیت نهایی یک رزرو یا تصمیم هم‌زمان ادمین دیگر را بازنویسی کند.

3. **هر وضعیت جدید رزرو که صندلی نگه می‌دارد باید به `SEAT_HOLDING_STATUSES`
   اضافه شود** (`bot/database/repositories/sessions.py`). این تنها تعریفِ
   «این رزرو صندلی گرفته» در کل سیستم است و شش کوئری ظرفیت از آن می‌خوانند.
   فراموش کردنش دقیقاً همان باگی است که در ۲۰۲۶-۰۹-۱۰ باعث شد «نیازمند
   اصلاح» صندلی مشتری را آزاد کند.

4. **پذیرش در ورودی یک وضعیت نیست — یک مهر زمانی است.**
   `set_checked_in()` فقط `checked_in_at` را پر می‌کند و رزرو `approved`
   می‌ماند. هرگز وضعیت را برای «استفاده شد» عوض نکن: هر کوئری درآمد و
   حاضرین روی `status = 'approved'` فیلتر می‌کند، پس آن بلیت از گزارش مالی
   حذف و صندلی‌اش آزاد می‌شود. دقیقاً همین در مسیر ربات اتفاق افتاده بود و
   تا v19 کشف نشد.

5. **هر چیزی که ادمین باید بتواند عوض کند، در جدول `settings` است، نه
   `.env`.** قیمت، ظرفیت، شماره کارت، همه‌ی متن‌های سایت، قالب پیام‌ها.
   `.env` فقط رازها و مسیرهاست (`BOT_TOKEN`، `JWT_SECRET`، `DB_PATH`).

6. **پروسه‌ی API هرگز مستقیم با تلگرام حرف نمی‌زند.** `bot.py` و
   `api/server.py` دو پروسه‌ی جدا هستند که فقط فایل SQLite را به اشتراک
   می‌گذارند. API برای فرستادن پیام تلگرام، یک ردیف در `bot_outbox`
   می‌نویسد و حلقه‌ی بات آن را تحویل می‌دهد.

7. **مایگریشن فقط افزایشی.** ستون جدید = یک `ALTER TABLE ADD COLUMN` در
   لیست نسخه‌دار داخل `bot/database/schema.py` + بالا بردن `SCHEMA_VERSION`.
   هیچ ستونی حذف یا بازنویسی نمی‌شود؛ وضعیت بازنشسته را فقط از خواندن
   کنار می‌گذاریم (مثل `awaiting_buyer_confirmation`).

8. **متن‌های کاربر در یک جا متمرکزند.** بات: `bot/texts/fa.py`. سایت:
   جدول `I18N` در `website/assets/js/site.js` برای متن ثابت، و کلیدهای
   `content_*` در `settings` برای متنی که ادمین باید بتواند عوض کند.
   متن فارسی را داخل منطق هاردکد نکن.

9. **مسیر فایل‌های رسانه‌ای که از API می‌آید نسبی است** (`media/…`). در
   فرانت‌اند همیشه از `pp()` (در `site.js`) رد کن، وگرنه بسته به عمق صفحه
   تصویر نمایش داده نمی‌شود. این باگ سه بار تکرار شده.

10. **فرانت‌اند build ندارد.** نه npm، نه bundler، نه فریمورک. تگ‌های
   `<script src>` با ترتیب ثابت: `app.js` قبل از `site.js`. کتابخانه‌ی
   جدید اضافه نکن مگر واقعاً ناچار باشی.

11. **هیچ `fetch` بدون `try/catch` در پنل ادمین.** خطای بلعیده‌شده دقیقاً
    شبیه «دکمه کار نمی‌کند» به نظر می‌رسد — همان چیزی که باعث شد نبودِ
    مسیر حذف رویداد هفته‌ها دیده نشود.

12. **قبل از هر کامیت، `cd bot && ENV=test python3 test_bot.py` باید
    ۱۰۰٪ سبز باشد.** تست هرگز روی دیتابیس واقعی اجرا نمی‌شود (خودش
    `ENV=production` را رد می‌کند و یک فایل موقت می‌سازد).

## تله‌های تکرارشونده‌ی این کدبیس

الگوهای خرابی که واقعاً در این پروژه رخ داده‌اند. اگر باگی داری، اول این‌ها
را رد کن — شرح کامل در `docs/09-troubleshooting.md`.

| علامت | علت واقعی |
|---|---|
| «دکمه هیچ کاری نمی‌کند» | مسیر API اصلاً وجود ندارد → ۴۰۴ خاموش + `fetch` بدون `catch` |
| صندلی بی‌دلیل آزاد می‌شود | وضعیت جدید در `SEAT_HOLDING_STATUSES` نیست |
| بلیت فروخته‌شده در گزارش مالی نیست | وضعیتش از `approved` خارج شده (کوئری‌های درآمد فقط `approved` را می‌شمارند) |
| یک بلیت دو بار وارد شد | دو مسیر پذیرش، دو منبع حقیقت — پذیرش باید فقط `checked_in_at` باشد |
| تصویر آپلودشده نمایش داده نمی‌شود | فراموشی `pp()` روی مسیر رسانه |
| صفحه‌ی عضوی با نام فارسی ۴۰۴ می‌دهد | مسیر URL قبل از تطبیق `unquote()` نشده |
| پیام «ذخیره شد» زیر بخش اشتباه | شناسه‌ی DOM از روی متن فارسی ساخته شده و خالی درآمده |
| تغییرات بعد از دیپلوی دیده نمی‌شود | کش مرورگر — `Ctrl+Shift+R` |
| یک تابع ناگهان `NameError` می‌دهد | `import` محلی داخل یک شاخه، نام ماژول را برای کل تابع محلی کرده |

## نقشه‌ی سریع کد

```
bot/
  bot.py                  ← پروسه‌ی ربات تلگرام (init_db در استارت)
  api/server.py           ← پروسه‌ی API (۶۷ مسیر، بدون فریمورک)
  services/               ← تمام منطق تجاری  ← تغییرات رفتاری اینجا
  database/repositories/  ← دسترسی به دیتابیس، یک ماژول برای هر جدول
  database/schema.py      ← تعریف ۲۴ جدول + مایگریشن‌ها + مقادیر پیش‌فرض
  handlers/               ← ۱۳ روتر aiogram (ترتیب ثبت در bot.py مهم است)
  texts/fa.py             ← همه‌ی متن‌های فارسی ربات
  test_bot.py             ← ۷۱ تست، بدون pytest
website/
  pages/                  ← ۱۰ صفحه‌ی عمومی + ۱۱ صفحه‌ی ادمین
  assets/js/app.js        ← لایه‌ی داده/API + احراز هویت + تم
  assets/js/site.js       ← رندر سایت عمومی + دوزبانگی + کل مودال رزرو
  assets/js/resume.js     ← ماژول مشترک رزومه (منصور + اعضا)
  assets/css/styles.css   ← تمام استایل‌ها + ۲۴ توکن طراحی
docs/                     ← مستندات کامل ← از docs/README.md شروع کن
```

## تعریف «کارِ تمام‌شده»

یک تغییر وقتی تمام است که هر پنج مورد انجام شده باشد:

1. کد نوشته شده و قانون‌های بالا را نشکسته.
2. `ENV=test python3 test_bot.py` سبز است — و اگر باگ رفع کرده‌ای، **تستی
   نوشته‌ای که روی کدِ قبلی fail می‌شود** (این را واقعاً امتحان کن، نه فرض).
3. یک ورودی در `CHANGELOG.md` بالای فایل: عنوان از دید کاربر، `**Why:**`،
   ریشه‌ی مشکل، فایل‌های تغییرکرده، و خط `Verified locally:` با شواهد واقعی.
4. اگر رفتار یا ساختاری عوض شده، سند مربوطه در `docs/` هم به‌روز شده.
5. اگر تغییر بک‌اند بوده، در پیام دیپلوی ذکر شده که ریستارت سرویس لازم است.

## سبک کد

- کامنت‌ها **«چرا» را توضیح می‌دهند، نه «چه»**. ۱۸٪ خطوط بک‌اند کامنت/داکسترینگ
  است و این عمدی است. وقتی گزینه‌ای را رد می‌کنی، دلیلش را بنویس.
- تکرار یک لیست/ثابت در چند جا ممنوع — یک تعریف مرجع بساز و import کن
  (درس گرفته‌شده از باگ `SEAT_HOLDING_STATUSES`).
- در `except` خالی رد نشو مگر دلیلش را کنارش بنویسی.
- نام فایل‌ها و شناسه‌ها انگلیسی، متن کاربر فارسی.
